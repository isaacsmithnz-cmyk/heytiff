/* WHAT THE MICROPHONE'S WAITS ARE CALLED.

   One string, five places. Reading a recording back happens on the capture
   sheet, on the debrief card, on all three note postures and under Tiff's ask
   bar, and it was written out by hand at every one of them — five literals
   that had already drifted in punctuation before anyone counted them.

   It lives beside the engine rather than with any surface because the wait
   belongs to the microphone, not to a screen. The library's own phase names
   are the mirror of that: they live with the machine that produces them
   (`workingNote`, lib/tiff/research-viz.ts), because retrieval is what puts
   us in them.

   NO PUNCTUATION. The ellipsis is the stylesheet's (`.orb-say b::after`), so
   a test matches the sentence and not a particular run of dots, and every
   wait in the app trails off the same way. */
export const READING_BACK_NOTE = "Reading it back";
